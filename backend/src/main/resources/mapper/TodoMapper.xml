<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.web.fitquest.mapper.todo.TodoMapper">
    <select id="getTodoList" resultType="com.web.fitquest.model.todo.Todo"> 
        SELECT id,user_id userId,category_id categoryId,is_done isDone,content,date FROM todo WHERE
        user_id = #{userId} AND date = #{date} 
    </select>

    <insert id="addTodo"> 
        insert into todo (user_id, category_id, content, date) 
        values (#{userId}, #{categoryId}, #{content}, #{date}) 
    </insert>

    <update id="updateTodo"> 
        update todo set 
        <if test="categoryId!=null">
            category_id = #{categoryId},
        </if>
        <if test="isDone!=null">
            is_done = #{isDone},
        </if>
        <if test="content!=null">
            content = #{content},
        </if>
        <if test="date!=null">
            date = #{date}
        </if>
        where id = #{id} 
    </update>

    <select id="getTodoListByYearAndUserId" resultType="com.web.fitquest.model.todo.Todo">
        SELECT * FROM todo WHERE
        user_id = #{userId} AND date LIKE CONCAT(#{date}, '%')
    </select>

    <select id="getTodoById" resultType="com.web.fitquest.model.todo.Todo">
        select id, user_id userId, category_id categoryId, is_done isDone, content, date from todo where id = #{id}
    </select>
    
    <delete id="deleteTodo">
        delete from todo where id = #{id}
    </delete>
</mapper>